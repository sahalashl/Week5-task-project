import React,{useEffect,useState} from 'react';import API from '../services/api';export default function Jobs(){const [jobs,setJobs]=useState([]);useEffect(()=>{API.get('/jobs').then(r=>setJobs(r.data)).catch(()=>{});},[]);return(<div><h2>Jobs</h2>{jobs.length===0&&<p>No jobs (or none approved yet).</p>}<ul>{jobs.map(j=><li key={j._id}><strong>{j.title}</strong> - {j.description} (Budget: {j.budget})</li>)}</ul></div>);}
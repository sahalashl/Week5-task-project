import React,{useEffect} from 'react';import {useNavigate} from 'react-router-dom';import {setAuthToken} from '../services/api';export default function Dashboard(){const nav=useNavigate();useEffect(()=>{const token=localStorage.getItem('token');if(token){setAuthToken(token);}else nav('/login');},[]);return(<div><h2>Dashboard</h2><p>Welcome to your dashboard. Use the Jobs page to view available jobs.</p></div>);}